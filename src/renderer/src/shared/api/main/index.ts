import {
  basename,
  convert,
  dirname,
  ffprobe,
  getInitialFilePath,
  onProgress,
  openDirectory,
  openFile,
  showProgress,
} from './main'
import { openPath } from './shell'

export const mainApi = {
  basename,
  convert,
  dirname,
  ffprobe,
  getInitialFilePath,
  onProgress,
  openDirectory,
  openFile,
  openPath,
  showProgress,
}
